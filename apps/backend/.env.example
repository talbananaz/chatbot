# Server Configuration
PORT=3000
FRONTEND_URL=http://localhost:5173

# AWS Bedrock Configuration
# Choose ONE of the following authentication methods:

# Option 1: AWS SSO Profile (recommended for development)
# AWS_PROFILE=your-sso-profile-name

# Option 2: Static Credentials (for CI/CD or production)
# AWS_ACCESS_KEY_ID=your-access-key-id
# AWS_SECRET_ACCESS_KEY=your-secret-access-key

# Option 3: Default Credentials (uses AWS SDK default credential chain)
# Leave both AWS_PROFILE and AWS_ACCESS_KEY_ID unset

# AWS Region
AWS_REGION=us-east-1

# Bedrock Model ID
BEDROCK_MODEL=anthropic.claude-3-5-sonnet-20241022-v2:0
