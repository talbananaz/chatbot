# AWS Bedrock Configuration
AWS_REGION=us-east-1

# Authentication Method 1: AWS SSO Profile (recommended)
# Uncomment to use an AWS SSO profile configured via `aws configure sso`
# AWS_PROFILE=your-sso-profile-name

# Authentication Method 2: Static Credentials
# Only use if not using AWS_PROFILE
# AWS_ACCESS_KEY_ID=your_access_key_here
# AWS_SECRET_ACCESS_KEY=your_secret_key_here

# Authentication Method 3: Default Credentials
# If neither AWS_PROFILE nor credentials are set, the AWS SDK will use:
# - Environment variables (AWS_ACCESS_KEY_ID, AWS_SECRET_ACCESS_KEY)
# - Shared credentials file (~/.aws/credentials)
# - EC2 instance metadata (if running on EC2)

# Bedrock Model Configuration
BEDROCK_MODEL=anthropic.claude-3-5-sonnet-20241022-v2:0

# Server Configuration
PORT=3000
FRONTEND_URL=http://localhost:5173
